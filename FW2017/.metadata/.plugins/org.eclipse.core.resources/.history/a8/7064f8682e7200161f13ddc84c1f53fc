/*
 * timer.cpp
 *
 *  Created on: Sep 3, 2016
 *      Author: James
 */

#include "timer.hpp"

namespace Peripherials
{
	typedef enum TimerClockSource
	{
		TAxCLK = 0,
		ACLK = 1,
		SMCLK = 2,
		INCLK = 3,
	} TimerClockSource;

	Timer TA0(TIMER_A0), TA1(TIMER_A1), TA2(TIMER_A2), TA3(TIMER_A3);

	Timer::Timer(Timer_A_Type* instance, TimerClockSource source) : regs(*instance)
	{
		regs.CTL =
	}
}
