/*
 * uart.hpp
 *
 *  Created on: Sep 10, 2016
 *      Author: James
 */

#ifndef UART_UART_HPP_
#define UART_UART_HPP_

#include "Buffers.hpp"
#include "msp.h"

namespace Peripherials
{
class UART
{
private:
	EUSCI_A_Type& regs;
	Buffers::RollingBuffer buffer;

public:
	UART(EUSCI_A_Type& instance, int baud, Buffers::RollingBuffer backing);
	void Send(char c);
	void Send(char[] str);
	//void Send(char* arr, int length);

	void OnInterrupt();
};

extern UART UART_A1;
}


#endif /* UART_UART_HPP_ */
